<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mindtalk.forum.modules.series.mapper.SeriesPostMapper">

    <select id="selectPostIdsBySeriesId" resultType="long">
        SELECT post_id FROM series_posts
        WHERE series_id = #{seriesId} AND deleted = 0
        ORDER BY sort_order ASC, id ASC
    </select>

    <select id="selectSeriesIdsByPostId" resultType="long">
        SELECT series_id FROM series_posts
        WHERE post_id = #{postId} AND deleted = 0
    </select>

</mapper>
