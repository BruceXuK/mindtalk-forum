<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mindtalk.forum.modules.message.mapper.MessageMapper">

    <select id="selectByConversationId" resultType="com.mindtalk.forum.modules.message.entity.Message">
        SELECT * FROM messages
        WHERE conversation_id = #{conversationId} AND deleted = 0
        ORDER BY create_time ASC
    </select>

    <select id="countUnread" resultType="int">
        SELECT COUNT(*) FROM messages m
        INNER JOIN conversations c ON c.id = m.conversation_id AND c.deleted = 0
        WHERE (c.user1_id = #{userId} OR c.user2_id = #{userId})
          AND m.sender_id != #{userId}
          AND m.is_read = FALSE
          AND m.deleted = 0
    </select>

</mapper>
