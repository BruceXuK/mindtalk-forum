<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mindtalk.forum.modules.subscription.mapper.CategorySubscriptionMapper">

    <select id="exists" resultType="boolean">
        SELECT COUNT(*) > 0 FROM category_subscriptions
        WHERE user_id = #{userId} AND category_id = #{categoryId} AND deleted = 0
    </select>

    <select id="selectSubscribedCategoryIds" resultType="long">
        SELECT category_id FROM category_subscriptions
        WHERE user_id = #{userId} AND deleted = 0
    </select>

</mapper>
