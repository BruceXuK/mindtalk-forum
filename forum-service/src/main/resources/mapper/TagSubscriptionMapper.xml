<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mindtalk.forum.modules.subscription.mapper.TagSubscriptionMapper">

    <select id="exists" resultType="boolean">
        SELECT COUNT(*) > 0 FROM tag_subscriptions
        WHERE user_id = #{userId} AND tag_id = #{tagId} AND deleted = 0
    </select>

    <select id="selectSubscribedTagIds" resultType="long">
        SELECT tag_id FROM tag_subscriptions
        WHERE user_id = #{userId} AND deleted = 0
    </select>

</mapper>
